export function ScuPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">SCU</h1>
      <p className="mt-2 text-slate-400">
        Send outbound DIMSE operations (C-ECHO, C-FIND, C-MOVE, C-GET,
        C-STORE) to a configured peer. Implementation lands in milestone M8.
      </p>
    </section>
  );
}
