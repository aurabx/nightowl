export function PeersPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Peers</h1>
      <p className="mt-2 text-slate-400">
        Configure remote DICOM nodes (AE Title, host, port) that this app can
        talk to. Implementation lands in milestone M7.
      </p>
    </section>
  );
}
