export function StorePage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Store</h1>
      <p className="mt-2 text-slate-400">
        Browse the local SOP Instance index (Patient / Study / Series /
        Instance). Implementation lands in milestone M2.
      </p>
    </section>
  );
}
